Flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
python-dotenv==1.0.1
numpy==1.26.4
gunicorn==22.0.0
# Optional — only used if a GEMINI_API_KEY is present. Safe to skip on ARM if wheels
# are unavailable; Vayu degrades gracefully to its local rule-based assistant.
google-genai==0.8.0
